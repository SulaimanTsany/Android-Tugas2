<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugasmobile.diss.tugas02sules">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pets"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_pets_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".DetailMovieActivity" />
        <activity
            android:name=".BottomNavagationActivity"
            android:label="@string/title_activity_bottom_navagation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>